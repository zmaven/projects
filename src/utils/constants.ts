const variants = {
    DEFAULT: 'default',
    PRIMARY: 'primary',
    SECONDARY: 'secondary'
};

const signInSteps = {
    LOGIN: 'login',
    EMAIL: 'email',
    PASSWORD: 'password',
    REMEMBER_ME: 'remember-me'
};

const passwordResetSteps = {
    DONE: 'done',
    RESET: 'reset',
    METHOD: 'method',
    VERIFY: 'verify'
};

const draftsTab = {
    SUITE: 'suite',
    TAKE_OFFS: 'take-offs'
};

const PROJECT_SLUG = 0;
const PROJECT_DETAILS_SLUG = 1;
const PHOTO_SLUG = 2;
const PHOTO_DETAILS_SLUG = 2;

export {
    variants,
    draftsTab,
    signInSteps,
    passwordResetSteps,
    PROJECT_SLUG,
    PROJECT_DETAILS_SLUG,
    PHOTO_SLUG,
    PHOTO_DETAILS_SLUG
};
