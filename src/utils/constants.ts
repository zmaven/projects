const variants = {
    DEFAULT: 'default',
    PRIMARY: 'primary',
    SECONDARY: 'secondary'
};

const signInSteps = {
    LOGIN: 'login',
    EMAIL: 'email',
    PASSWORD: 'password',
    REMEMBER_ME: 'remember-me'
};

const passwordResetSteps = {
    DONE: 'done',
    RESET: 'reset',
    METHOD: 'method',
    VERIFY: 'verify'
};

const draftsTab = {
    SUITE: 'suite',
    TAKE_OFFS: 'take-offs'
};

export { variants, draftsTab, signInSteps, passwordResetSteps };
